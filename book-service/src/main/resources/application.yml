server:
  port: 8100
spring:
  application:
    name: book-service
  datasource:
    url: jdbc:mysql://localhost:3306/book-service?useTimezone=true&serverTimezone=UTC
    username: root
    password: 1804
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

resilience4j:
  retry:
    instances:
      default:
        max-attempts: 5
        wait-duration: 1s
